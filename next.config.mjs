/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['s3-alpha-sig.figma.com','media.licdn.com','udemy-certificate.s3.amazonaws.com'],
    }
};

export default nextConfig;
